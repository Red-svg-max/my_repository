name: Github Actions
on: [push, pull_request]
jobs:
  Explore-Github-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Run only on main
        if: github.ref == 'refs/heads/no-ga'
        run: echo "cette branche ne doit pas être utilisée"
             exit 1
      - name: checking_repository_name
        if: github.ref == 'git@github.com:EpitechPGE1-2025/G-DOP-200-RUN-2-1-chocolatine-1.git'
        run: exit 1

